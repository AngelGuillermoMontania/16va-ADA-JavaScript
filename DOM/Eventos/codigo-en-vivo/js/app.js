const button = document.querySelector("#primer-boton")

/* button.addEventListener("click", (event) => {
    console.log(event)
}) */

/* window.addEventListener("mousemove", (e) => {
    console.log(e)
}) */

window.addEventListener("keypress", (event) => {
    console.log(event)
})